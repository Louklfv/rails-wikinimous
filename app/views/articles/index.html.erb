<div class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
  <h2>List of articles</h2>
  </div>
</div>
<div class="container">
  <% @articles.each do |article| %>
  <h3><%= link_to article.title, article_path(article) %><%= link_to "🗑", article_path(article), method: :delete, data: { confirm: "Are you sure?" } %><%= link_to "🆙", edit_article_path(article) %></h3>
  <% end %>
  <h3><%= link_to "➕ Add a new Article", new_article_path %></h3>
</div>
