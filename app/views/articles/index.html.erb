<h1>Articles :</h1>
<% @articles.each do |article| %>
<h3><%= link_to article.title, article_path(article) %><%= link_to "🗑", article_path(article), method: :delete, data: { confirm: "Are you sure?" } %></h3>
<% end %>
<h3><%= link_to "➕ Add a new Article", new_article_path %></h3>
